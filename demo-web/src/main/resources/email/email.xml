<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd"
       default-lazy-init="true">

    <description>Email配置</description>

    <!-- 自定义基于velocity的模板解析 -->
    <bean id="velocityTemParse" class="com.muses.utils.VelocityTemplateUtils" init-method="initEngine"/>
    <bean id="jDomParse" class="com.muses.utils.JDomParseUtils"
          init-method="initLoadXml">
        <property name="mailTemplate" value="classpath:velocity/mailVelocity.xml"/>
    </bean>

    <!-- 邮件发送器 -->
    <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="${mail.host}"/>
        <property name="username" value="${mail.username}"/>
        <property name="password" value="${mail.password}"/>
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">true</prop>
                <!-- 使用gmail smtp server的必须参数 -->
                <prop key="mail.smtp.starttls.enable">true</prop>
            </props>
        </property>
    </bean>

    <!-- 文本邮件服务类 -->
    <bean id="simpleMailService" class="com.muses.send.email.SimpleMailService">
        <property name="mailSender" ref="mailSender"/>
        <property name="textTemplate">
            <value><![CDATA[
             用戶%1$s在%2$tF被修改.
                     System Administrator.
             ]]></value>
        </property>
    </bean>

    <!-- MIME邮件服务类 -->
    <bean id="mimeMailService" class="com.muses.send.email.MimeMailService">
        <property name="mailSender" ref="mailSender"/>
        <property name="velocityEngine" ref="mailVelocityEngine"/>
        <property name="templateFileName" value="mailTemplate.vm"/>
    </bean>

    <!-- VelocityEngine -->
    <bean id="mailVelocityEngine" class="org.springframework.ui.velocity.VelocityEngineFactoryBean">
        <property name="resourceLoaderPath" value="classpath:/velocity/"/>
    </bean>

</beans>