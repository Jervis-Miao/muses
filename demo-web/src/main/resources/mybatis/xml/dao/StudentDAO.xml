<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.muses.repository.dao.StudentDAO">
  <select id="selectStudentsByIds" resultMap="com.muses.repository.mapper.StudentMapper.BaseResultMap">
    select stu.student_id, stu.name, stu.mobile
      from ins.student stu
     where stu.student_id in
      <foreach item="item" index="index" open="(" separator="," close=")" collection="ids">
        #{item}
      </foreach>
  </select>
</mapper>